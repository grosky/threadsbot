# Lazy Threads — TODO

Список задач, отсортирован по приоритету. Обновляй по ходу — пусть будет в git, легко отслеживать.

> **Для нового чата с Claude:** прочитай этот файл целиком + `README.md` + `APP_REVIEW.md`. Архитектура продукта и весь контекст изложен в разделе «✅ Сделано» ниже. Текущая модель — `gemini-3-flash-preview` с fallback на `2.5-flash`. Деплой — Railway, домен `worker-production-5690.up.railway.app`. Threads-публикация работает но скрыта за фича-флагом `THREADS_PUBLISH_ENABLED` (по умолчанию false) — ждём App Review от Meta.

---

## 🔴 Высокий приоритет (продуктовый рост)

### UX-пасс по всем сообщениям бота
Партия 1 (8 экранов):
- ✅ Welcome screen — переписан как продающая копия + картинка-социалка
- Главное меню + статус-блок
- Подменю Создание / Аналитика / Настройки
- Сравнение форматов
- Карточки сгенерированных постов
- Результат голосового сторителлинга
- Превью «свой пост»
- ✅ Сообщение после онбординга — обновлено (вопрос «есть Threads-профиль?» + предложение упаковки)

Партия 2 (~5 экранов):
- Анализ профиля / Разбор ленты (intro + результат)
- Threads connect (подключён / не подключён)
- Achievements экран
- Error-сообщения (унифицировать стиль)
- OAuth-callback страницы

### Gemini Pro для платных
**Отложено** — Gemini 3 Pro ещё не вышел в стабильной версии. Возвращаемся когда выйдет либо когда Google сделает 3-pro-preview доступным. План: подписчики получат 3-pro, free trial остаётся на 3-flash. Архитектурно `_call_with_fallback` уже позволяет передать tier — рабочая ветка лежит в `gemini_service.py`.

### Команда `/delete_me`
Обещана в `privacy.html#data-deletion` перед Meta App Review. Полное удаление юзера из БД с подтверждением. ~30 минут кода.

---

## 🟡 Средний приоритет

### Meta App Review
Готовы:
- ✅ Privacy Policy + ToS на GitHub Pages (с разделом #data-deletion)
- ✅ Описание use case (в `APP_REVIEW.md`)
- ✅ Permission descriptions (готовые тексты)
- ✅ Иконка 1024×1024 (загружена)
- ✅ App Settings → Basic заполнен в Meta dashboard
- ✅ Permissions добавлены в Use Cases: `threads_basic` + `threads_content_publish`

**Блокер найден:** Meta требует **Tech Provider** статус для submit'а на App Review — это включает Business Verification (ИП/ООО). У владельца ИП есть → процесс может быть пройден, но это +2-4 недели. **Отложено до отдельного спринта.**

Осталось:
- ❌ Добавить `threads_manage_replies` в Use Cases (используется в коде для длинных постов)
- ❌ Демо-видео 2-3 мин (сценарий в `APP_REVIEW.md`)
- ❌ Tech Provider: подать документы ИП (ЕГРИП, ИНН, выписка из банка)
- ❌ Submit for Review
- ❌ Дождаться одобрения и флипнуть `THREADS_PUBLISH_ENABLED=true`

### UTM-генератор для админа
- Команда `/utm` → бот спрашивает `source`
- Возвращает ссылку `t.me/aithreadbot?start=ref_<admin_id>_<source>`
- Сохраняем `source` в `referrals` таблице
- `/stats <source>` → «N кликов, M активаций, K оплат»

### Инструкции в боте
- Записать скринкаст или 5-7 скринов:
  - /start → Welcome
  - Free trial → онбординг
  - Меню → Создание → Сгенерить
  - Получение поста + post-actions
  - Подключение Threads (OAuth)
  - Опубликованный пост
- Команда `/help` показывает их юзеру

---

## 🟢 Низкий приоритет (когда есть время)

### Тон обращения «ты» — унификация
Где-то «вы», где-то «ты». Привести к единому «ты».

### Compact OAuth для Threads
Сейчас юзер копирует ссылку в браузер. После App Review — можно делать OAuth прямо в Telegram через Web App.

### История постов
- `/history` или кнопка «📚 Мои посты»
- Список последних 20 постов
- Кнопка «опубликовать заново»

### Trial-период через Tribute
Добавить 3-дневный free trial в Subscription product → выше конверсия.

### Push-уведомления
- ✅ Догревочная цепочка — 3 сообщения после `/start` без оплаты (15м/1ч/3ч)
- «Напомню сгенерить пост утром» — расписание
- «Срок подписки истекает через 3 дня» — авто-напоминание

### Daily challenge (геймификация)
Раз в день случайная задача: «Сегодня попробуй формат "Контринтуитивный"». Бьёт по retention.

---

## 🛠 Технический долг

- bot.db живёт в `/data` (Railway Volume) — backup стратегия не настроена
- Логи Railway хранятся 7 дней — для аудита нужен внешний sink (LogTail/Datadog)
- Token refresh для Threads — long-lived живёт 60 дней, нужен фоновый job для refresh

---

## ✅ Сделано (для истории)

- Бот хостится на Railway
- Threads OAuth + публикация коротких постов
- Threads thread chain (через `threads_manage_replies`)
- Tribute оплаты + автопродление подписки
- Реферальная программа
- 9 ачивок + стрики
- Анализ профиля по скрину
- Разбор чужой ленты
- Голосовой сторителлинг
- Свой пост в Threads
- Free trial для новых юзеров
- Welcome screen + paywall
- Privacy + ToS на GitHub Pages
- Иерархическое меню (3 раздела)
- **Рандомизация форматов** — кнопки «Короткий / Длинный тред», бот сам микширует 3 разных формата
- **Упаковка профиля для новичков** — кнопка «🆕 Упаковать профиль» в Создание + автопредложение после онбординга. Генерит имя/bio/ссылку/закреп одним Gemini-запросом, каждая карточка имеет 🔄 для перегенерации, результат хранится в `profile_pack_json`, доступ по `/mypack`. Гейтинг: подписка/триал, дневной лимит не списывается.
- **Продающий welcome screen** — переписан с кейсами (12k/2 недели, 2500+600к, 1000 TG, 1800 лидов) + социальное доказательство картинкой (`assets/welcome_proof.jpg`) перед текстом
- **Privacy: раздел Data Deletion** — добавлен `#data-deletion` anchor с `/delete_me` и email-фоллбэком (для Meta App Review)
- **Догревочная цепочка после /start** — 3 сообщения через 15м/1ч/3ч с кейсами и ссылками на залетевшие посты. Фоновый loop в `followups.py`, БД хранит mask + start_at в `users`. Отмена при оплате/блокировке/`/stop_followups`. Skip при опоздании > 2ч.
